# CLAUDE.md

This repository's agent-facing instructions live here. `AGENTS.md` points to this file so both conventions stay aligned.

## Agent skills

### Issue tracker

Issues and PRDs are tracked in GitHub Issues for `AliSalman-et-al/ARBITER`. See `docs/agents/issue-tracker.md`.

### Triage labels

Triage labels use the default vocabulary except `ready-for-agent` maps to `ready-to-implement`. See `docs/agents/triage-labels.md`.

### Domain docs

This is a single-context repo: root `CONTEXT.md` plus root `docs/adr/`. See `docs/agents/domain.md`.
